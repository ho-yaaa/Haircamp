{
  "name": "haircamp-figma-generator",
  "version": "1.1.0",
  "private": true,
  "description": "HairCamp detail page Figma generator plugin.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.130.0",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2"
  }
}
